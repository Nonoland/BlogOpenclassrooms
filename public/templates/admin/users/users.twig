{% extends '/admin/page.twig' %}

{% block page_title %}
    Liste des utilisateurs
{% endblock %}

{% block page_content %}
    <a href="{{ route('admin_users_new') }}">Nouveau utilisateur</a>

    <table class="table table-hover my-0">
        <thead>
        <tr>
            <th>ID</th>
            <th>Nom</th>
            <th>Prénom</th>
            <th>Email</th>
            <th>Roles</th>
            <th>Action</th>
        </tr>
        </thead>

        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.lastname }}</td>
                <td>{{ user.firstname }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.roles }}</td>
                <td>
                    <a href="/admin/users/edit/{{ user.id }}">Editer</a>
                    <a href="/admin/users/delete/{{ user.id }}">Supprimer</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    </section>

{% endblock %}
